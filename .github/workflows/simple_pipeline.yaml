name: Shell Commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps: 
      - name: echoes a string; this should now fail!
        run: eccho "Hello, there"
      - name:  multiline script 
        run: |
          node -v
          npm -v
